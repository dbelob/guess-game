package guess.domain;

/**
 * Application state.
 */
public enum State {
    START_STATE,
    GUESS_NAME_STATE,
    GUESS_PICTURE_STATE,
    GUESS_TALK_STATE,
    GUESS_SPEAKER_STATE,
    RESULT_STATE
}
