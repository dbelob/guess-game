package guess.domain.statistics.olap;

public enum Measure {
    DURATION,
    EVENTS_QUANTITY,
    TALKS_QUANTITY,
    SPEAKERS_QUANTITY,
    JAVA_CHAMPIONS_QUANTITY,
    MVPS_QUANTITY,
    EVENT_TYPES_QUANTITY
}
