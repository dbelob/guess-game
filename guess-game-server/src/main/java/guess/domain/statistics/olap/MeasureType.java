package guess.domain.statistics.olap;

/**
 * Measure type.
 */
public enum MeasureType {
    DURATION,
    EVENT_TYPES_QUANTITY,
    EVENTS_QUANTITY,
    TALKS_QUANTITY,
    SPEAKERS_QUANTITY,
    JAVA_CHAMPIONS_QUANTITY,
    MVPS_QUANTITY
}
